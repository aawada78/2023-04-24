import { ShowcaseDirective } from './showcase.directive';

describe('ShowcaseDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowcaseDirective();
    expect(directive).toBeTruthy();
  });
});
